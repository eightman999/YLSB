# YLSB v0.4-rc1 fixture set

The 28 UME core cases and four sentinels are adapted by `ylsb_v04.fixtures` from
the immutable v0.3 files. Every case carries `fixture_id`, `fixture_version`,
`benchmark_version`, `sha256`, `tier`, and `gate_roles`. `CORE-CONSTRAINT-003`
is corrected only in this version; the v0.3 case remains historical and audits as
`FIXTURE_INVALID`.
